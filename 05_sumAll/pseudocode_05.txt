
create a sumAll function
set two parameters: num1, num2

if num1 is NOT a number OR num2 is NOT a number THEN 
    RETURN "Error, is not a number"

let getMin = num1 < num2 ? num1 : num2
let getMax = num1 > num2 ? num1 : num2

let totalSum = 0
let i

Loop from getMin to getMax
    totalSum += i

RETURN totalSum

